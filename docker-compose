version: '3.9'

services:

  app:
    image: ghcr.io/csa20121996/mlops-cats-dogs:${IMAGE_TAG}
    ports:
      - "8000:8000"
    restart: always

  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
